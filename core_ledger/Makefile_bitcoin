CXX = g++
CXXFLAGS = -std=c++17 -Wall -O2
LDFLAGS = -lssl -lcrypto

bitcoin_blockchain: bitcoin_blockchain.cpp
	$(CXX) $(CXXFLAGS) -o bitcoin_blockchain bitcoin_blockchain.cpp $(LDFLAGS)

clean:
	rm -f bitcoin_blockchain bitcoin_blockchain.exe

run: bitcoin_blockchain
	./bitcoin_blockchain
